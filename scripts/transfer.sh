#!/bin/bash

# creates mirrored directories where all file contents are hashes of the files (with the same metadata?) and then diffs the directories to find out which files are _actually_ different since everything other than diff sucks

diff 
